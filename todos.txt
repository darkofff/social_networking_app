# To deploy website on gh pages just type 'npm run deploy'
    - but before all errors have to be fixed
    - some can be avoided by changing Linting in tsconfig.json to false
    - but there are also errors from supabase types
        
# Fix the typescript issues with supabase in:
    - replace all the any types with supabase generated types
    


# Form validation



# Do zrobienia
    - W search nie wyświetlają się osoby, których status znajomości jest niezaakceptowany
    - 
    -
    - Form validation
